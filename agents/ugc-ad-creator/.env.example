# Optional. Portraits, talking heads and audio use the platform's built-in media
# generation (no key needed). Set this only to generate SFX / music through
# ElevenLabs directly instead of the platform fallback (stable-audio-2.5 / lyria-3).
ELEVEN_LABS_API_KEY=
