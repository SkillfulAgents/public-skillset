# Animation clips

Self-contained HTML clips following the `window.ANIM` + `window.seek(t)` contract (see the `build-product-visual` skill). Design size 960×540 unless noted.

| File | Params | Duration | Best moment |
|---|---|---|---|
| status-flip.html | title, sub, from, to, color (hex, URL-encoded) | 6s | badge flips at 2.4s — start the scene ~1.6s before the VO says the outcome |
