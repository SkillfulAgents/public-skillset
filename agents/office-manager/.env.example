# No API keys are required for the core office-manager flows.
# Shopping sites (Amazon / Instacart / DoorDash / Uber Eats) are logged into via the
# built-in browser during onboarding, and Slack is connected as a chat integration.
# If you later add skills that need keys, list them here and copy this file to .env.
